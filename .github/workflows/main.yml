name: vue CI

on:
  push:
    branches:
      – master
  pull_request:
    branches:
      - master


jobs:
  build:
    runs-on: unbuntu-last

    steps:
    - name: 检查代码
      uses: actions/checkout@v4
    - name: 执行
      uses: actions/setun-node@v2

    - name: 完毕
      run: echo "finish."
      
